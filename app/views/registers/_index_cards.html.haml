- if registers.empty?
  %div{style: "padding-top:15px;"}
    = render "common/empty_list_message", model: Register
.columns
  - registers.each do |register|
    .event-card{style: "display: flex; flex-direction: column; "}
      = link_to interactuable_path(register), class: "hover-border" do
        .item-name{style: "width: 190px; height: 30px;", class: "soft-color"}
          %div{style: "display: flex; align-items: center; justify-content: flex-start; gap: 8px;"}
            .icon{style: "width: 25px; height: 25px; margin-left: 5px;"}
              = image_tag register.user.icon, style: "width: 100%; height: 100%; object-fit: cover;" if register.user.icon.attached?
            %span= register.user.username
        .rectangle_icon{style: "width: 190px; height: 150px;"}
          = image_tag register.event.get_photo if register.event.get_photo.present?
        .item-name{style: "width: 190px;"}
          = register.event.tour_name
      .title_section
        %div
          =register.get_rating
          - if register.review.present?
            %span{style: "font-size:12px;"} Ξ 
        %span{style: "font-size:12px;"}= short_date(register.created_at)