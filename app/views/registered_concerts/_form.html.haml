= form_with model: @registered_concert, url: registered_concerts_path do |f|
  - puts @concert.inspect
  = f.hidden_field :concert_id
  = f.hidden_field :user_id, value: current_user&.id

  = f.text_field :text
  = f.submit "Guardar"